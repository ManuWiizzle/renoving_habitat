<form method="post" class="gooddeal_delete_form mx-auto mt-4" action="{{ path('good_deal_delete', {'id': good_deal.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet article?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ good_deal.id) }}">
    <button class="sendButtonRed">Supprimer</button>
</form>
